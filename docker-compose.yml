version: '3.9'

services:
  app:
    build:
      context: ./docker/app
    ports:
      - '80:80'
      - '5173:5173'
    volumes:
      - ./src/:/var/www/html
      - ./docker/apache/default.conf:/etc/apache2/sites-enabled/000-default.conf

  db:
    build: ./docker/db
    volumes:
      - db-store:/var/lib/mysql

volumes:
  db-store:
